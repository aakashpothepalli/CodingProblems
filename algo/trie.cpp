#include<bits/stdc++.h>
#define ll long long
#define ull unsigned long long
#define  in(ar,n) for(int i=0;i<(int)n;i++)cin>>ar[i];
#define out(ar,n)     for(int i=0;i<(int)n;i++)     cout<<ar[i]<<" ";     cout<<endl;
#define for2d(n,m) for(int i=0;i<(int)n;i++)for(int j=0;j<(int)m;j++)
#define fori(i,a,b) for(ll i=a;i<(int)b;++i)
#define all(ar) ar.begin(),ar.end()
using namespace std;
typedef pair<ll,ll> pll;
typedef pair<int,int> pii;
typedef vector<vector<int>> vvi;
const ll mod = 1e9 + 7;

int main(){
    // int t;cin>>t;
    // while(t--){
        int n;cin>>n;
        ll ar[n];
        in(ar,n);
        
    // }

}

/*
use a vector if you plan to change elements while iterating through a map
use a priority queue if you wish to change elements while iterating through a set
use a multiset if your array contains duplicates
*/